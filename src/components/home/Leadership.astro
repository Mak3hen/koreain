<section class="section leadership" id="leadership" aria-labelledby="leadership-title">
  <div class="container leadership__grid">
    <figure class="leadership-portrait">
      <div
        class="leadership-portrait__art"
        role="img"
        aria-label="Portrait placeholder for Yaser Khalifa"
      >
        <span class="leadership-portrait__orbit" aria-hidden="true"></span>
        <span class="leadership-portrait__initials" aria-hidden="true">YK</span>
        <span class="leadership-portrait__accent" aria-hidden="true"></span>
      </div>
      <figcaption>Portrait placeholder</figcaption>
    </figure>

    <div class="leadership__content">
      <p class="eyebrow">At the helm</p>
      <h2 class="section-title" id="leadership-title">Leadership</h2>
      <p class="leadership__intro">
        KoreaIN is led with a focus on building meaningful, well-informed
        connections between Korea and international markets.
      </p>

      <div class="leader">
        <h3>Yaser Khalifa</h3>
        <p class="leader__role">CEO, KoreaIN</p>
        <p class="leader__credential">
          Member of the Saudi-Korea Business Council
        </p>
      </div>

      <a class="arrow-link leadership__link" href="/about#leadership">
        <span>Meet Our Leadership</span>
        <svg viewBox="0 0 20 20" aria-hidden="true">
          <path d="M4 10h11M11 5l5 5-5 5" />
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  .leadership {
    background: var(--color-surface);
  }

  .leadership__grid {
    display: grid;
    grid-template-columns: minmax(0, 1.12fr) minmax(22rem, 0.88fr);
    gap: clamp(4rem, 8vw, 8.5rem);
    align-items: center;
  }

  .leadership-portrait {
    margin: 0;
  }

  .leadership-portrait__art {
    position: relative;
    display: grid;
    min-height: clamp(40rem, 58vw, 50rem);
    overflow: hidden;
    place-items: end start;
    border-radius: var(--radius-xl);
    isolation: isolate;
    background:
      linear-gradient(145deg, rgba(0, 71, 160, 0.82), rgba(0, 14, 138, 0.28) 56%),
      #050722;
  }

  .leadership-portrait__art::before {
    position: absolute;
    inset: 11% 9% auto auto;
    width: 46%;
    aspect-ratio: 1;
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 50%;
    content: '';
  }

  .leadership-portrait__art::after {
    position: absolute;
    right: -13%;
    bottom: -9%;
    width: 68%;
    aspect-ratio: 1;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.055);
    content: '';
    z-index: -1;
  }

  .leadership-portrait__orbit {
    position: absolute;
    top: 18%;
    right: 16%;
    width: 28%;
    aspect-ratio: 1;
    border: 1px solid rgba(255, 255, 255, 0.32);
    border-radius: 50%;
  }

  .leadership-portrait__orbit::after {
    position: absolute;
    top: 50%;
    left: -0.3rem;
    width: 0.58rem;
    height: 0.58rem;
    border-radius: 50%;
    background: #cd2e3a;
    content: '';
    transform: translateY(-50%);
  }

  .leadership-portrait__initials {
    padding: clamp(2rem, 5vw, 4.25rem);
    color: #fff;
    font-size: clamp(7rem, 17vw, 14rem);
    font-weight: 650;
    line-height: 0.72;
    letter-spacing: -0.095em;
  }

  .leadership-portrait__accent {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0.55rem;
    height: 28%;
    background: #cd2e3a;
  }

  .leadership-portrait figcaption {
    margin-top: 0.8rem;
    color: var(--color-muted);
    font-size: 0.72rem;
    font-weight: 650;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .leadership__content .section-title {
    margin-top: 1rem;
  }

  .leadership__intro {
    max-width: 39rem;
    margin: clamp(2rem, 3.5vw, 2.75rem) 0 0;
    color: var(--color-muted);
    font-size: clamp(1.08rem, 1.55vw, 1.28rem);
    line-height: 1.75;
  }

  .leader {
    margin-top: clamp(2.75rem, 4.5vw, 4rem);
    padding-top: clamp(2rem, 3.5vw, 2.75rem);
    border-top: 1px solid var(--color-border);
  }

  .leader h3 {
    margin: 0;
    color: var(--color-text);
    font-size: clamp(1.75rem, 3vw, 2.6rem);
    font-weight: 620;
    line-height: 1.1;
    letter-spacing: -0.04em;
  }

  .leader__role {
    margin: 0.55rem 0 0;
    color: var(--color-primary);
    font-size: 0.92rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .leader__credential {
    max-width: 28rem;
    margin: 1.25rem 0 0;
    color: var(--color-text);
    font-size: 1rem;
    line-height: 1.65;
  }

  .leadership__link {
    margin-top: 3rem;
  }

  .leadership__link svg {
    width: 1.15rem;
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.7;
  }

  @media (max-width: 60rem) {
    .leadership__grid {
      grid-template-columns: minmax(0, 0.9fr) minmax(18rem, 1fr);
      gap: clamp(2.5rem, 6vw, 4.5rem);
    }

    .leadership-portrait__art {
      min-height: clamp(34rem, 64vw, 43rem);
    }
  }

  @media (max-width: 47rem) {
    .leadership__grid {
      grid-template-columns: 1fr;
    }

    .leadership-portrait {
      max-width: 34rem;
    }

    .leadership-portrait__art {
      min-height: auto;
      aspect-ratio: 4 / 4.6;
    }

    .leadership__content {
      max-width: 40rem;
    }
  }
</style>
