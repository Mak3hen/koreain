<div class="hero">
  <video
    class="hero__video"
    autoplay
    muted
    loop
    playsinline
    preload="metadata"
    aria-hidden="true"
  >
    <source src="/homepage.mp4" type="video/mp4" />
  </video>
  <div class="hero__shade" aria-hidden="true"></div>
  <div class="hero__signature">
    <p class="hero__eyebrow">
      <span>KOREA</span>
      <span class="hero__eyebrow-line separator-line" aria-hidden="true">
        <svg viewBox="0 0 44 2" fill="none" preserveAspectRatio="none">
          <line x1="0" y1="1" x2="44" y2="1" stroke="#CD2E3A" stroke-width="2" stroke-linecap="round" />
        </svg>
      </span>
      <span>GLOBAL</span>
    </p>
    <h1 class="hero__tagline">Connecting Korea to Global Partners</h1>
  </div>
</div>

<style>
  .hero {
    position: relative;
    width: 100%;
    height: calc(100svh - var(--header-height));
    /* Offset the fixed header; together, header and hero fill one small viewport. */
    margin: var(--header-height) 0 0;
    padding: 0;
    overflow: hidden;
    background: #050722;
  }

  .hero__video {
    position: absolute;
    z-index: 0;
    inset: 0;
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    /* Favor the upper skyline while allowing full-bleed edge cropping. */
    object-position: 50% 25%;
  }

  @media (max-width: 47.99rem) {
    .hero__video {
      object-position: 62% 25%;
    }
  }

  .hero__shade {
    position: absolute;
    z-index: 1;
    inset: auto 0 0;
    height: 48%;
    background: linear-gradient(
      to bottom,
      rgba(5, 7, 34, 0),
      rgba(5, 7, 34, 0.12) 40%,
      rgba(5, 7, 34, 0.42)
    );
    pointer-events: none;
  }

  .hero__signature {
    position: absolute;
    z-index: 2;
    left: clamp(24px, 5vw, 96px);
    bottom: max(clamp(32px, 8vh, 96px), env(safe-area-inset-bottom, 0px));
    width: 100%;
    max-width: clamp(320px, 46vw, 720px);
    animation: hero-signature-enter 650ms cubic-bezier(0.22, 1, 0.36, 1) both;
  }

  .hero__eyebrow {
    display: inline-flex;
    align-items: center;
    gap: clamp(0.75rem, 1vw, 1rem); /* 12–16px */
    margin: 0 0 1.125rem;
    color: rgba(255, 255, 255, 0.92);
    font-size: clamp(1rem, 0.4vw + 0.9rem, 1.125rem); /* 16-18px desktop */
    font-weight: 600;
    letter-spacing: 0.18em;
    line-height: 1;
    text-transform: uppercase;
  }

  .hero__eyebrow-line,
  .separator-line {
    display: inline-flex !important;
    align-items: center !important;
    width: clamp(2.25rem, 2.8vw, 3rem) !important; /* 36–48px desktop */
    height: 2px !important;
    min-width: 36px;
    min-height: 2px;
    flex: 0 0 auto !important;
    background-color: #cd2e3a !important;
    border-radius: 1px;
    vertical-align: middle;
  }

  .hero__eyebrow-line svg,
  .separator-line svg {
    display: block;
    width: 100%;
    height: 2px;
  }

  .hero__tagline {
    margin: 0;
    color: #fff;
    font-size: clamp(3.5rem, 4.3vw, 4.5rem); /* 56-72px desktop */
    font-weight: 600;
    letter-spacing: -0.03em;
    line-height: 1.05;
    text-wrap: balance;
    text-shadow: 0 2px 20px rgba(5, 7, 34, 0.4);
  }

  @keyframes hero-signature-enter {
    from {
      opacity: 0;
      transform: translateY(14px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Tablet */
  @media (max-width: 63.99rem) {
    .hero__eyebrow {
      font-size: clamp(0.875rem, 1.2vw + 0.6rem, 1rem); /* 14-16px tablet */
      margin-bottom: 1rem;
      gap: 0.8125rem; /* ~13px */
    }

    .hero__eyebrow-line,
    .separator-line {
      width: 2.125rem !important; /* ~34px tablet */
      min-width: 30px;
    }

    .hero__tagline {
      font-size: clamp(2.625rem, 5.2vw, 3.25rem); /* 42-52px tablet */
      line-height: 1.06;
    }
  }

  /* Mobile */
  @media (max-width: 39.99rem) {
    .hero__signature {
      left: 20px;
      right: 20px;
      width: auto;
      max-width: calc(100% - 40px);
      bottom: max(36px, env(safe-area-inset-bottom, 0px));
    }

    .hero__eyebrow {
      font-size: clamp(0.8125rem, 2.5vw, 0.875rem); /* 13-14px mobile */
      margin-bottom: 0.75rem;
      gap: 0.75rem; /* 12px */
    }

    .hero__eyebrow-line,
    .separator-line {
      width: 1.875rem !important; /* 30px mobile */
      min-width: 24px;
    }

    .hero__tagline {
      font-size: clamp(2rem, 7.8vw, 2.5rem); /* 32-40px mobile */
      line-height: 1.08;
    }
  }

  /* Short-height displays */
  @media (max-height: 45rem) {
    .hero__signature {
      bottom: max(1.75rem, env(safe-area-inset-bottom, 0px));
    }

    .hero__eyebrow {
      margin-bottom: 0.625rem;
      font-size: 0.875rem;
    }

    .hero__tagline {
      font-size: clamp(2.125rem, 6vh, 3rem);
      line-height: 1.06;
    }
  }

  @media (max-height: 34rem) {
    .hero__signature {
      bottom: max(1.25rem, env(safe-area-inset-bottom, 0px));
    }

    .hero__eyebrow {
      margin-bottom: 0.5rem;
      font-size: 0.8125rem;
    }

    .hero__tagline {
      font-size: clamp(1.75rem, 7vh, 2.25rem);
      line-height: 1.08;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero__signature {
      animation: none;
    }
  }
</style>
