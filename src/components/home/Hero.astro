<div class="hero">
  <video
    class="hero__video"
    autoplay
    muted
    loop
    playsinline
    preload="metadata"
    aria-hidden="true"
  >
    <source src="/homepage.mp4" type="video/mp4" />
  </video>
</div>

<style>
  .hero {
    position: relative;
    width: 100%;
    height: clamp(48rem, 94svh, 64rem);
    margin: 0;
    padding: 0;
    overflow: hidden;
    background: #050722;
  }

  .hero__video {
    position: absolute;
    inset: 0;
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    /* Keep the source's top edge below the fixed navigation. */
    object-position: 50% var(--header-height);
  }

  @media (max-width: 47.99rem) {
    .hero {
      height: max(43rem, 88svh);
    }

    .hero__video {
      object-position: 62% var(--header-height);
    }
  }

  @media (min-width: 1024px) {
    .hero {
      height: auto;
      /* Reserve space for the fixed header without shifting or cropping the video. */
      margin-top: var(--header-height);
      overflow: visible;
    }

    .hero__video {
      position: static;
      inset: auto;
      height: auto;
      aspect-ratio: auto 16 / 9;
      object-fit: contain;
      object-position: center;
    }
  }
</style>
