<section class="section who" aria-labelledby="who-title">
  <div class="container who__grid">
    <figure class="who__visual">
      <img
        src="/images/whoweare-premium.png"
        alt="A conceptual bridge connecting Korean and Middle Eastern cityscapes."
        width="1678"
        height="937"
        loading="lazy"
        decoding="async"
      />
    </figure>

    <div class="who__content">
      <h2 id="who-title" class="section-title">Who are we?</h2>
      <p class="section-copy who__copy">
        KoreaIN connects Korean companies, expertise and business opportunities
        with trusted international partners. We identify shared commercial
        interests, cultivate key relationships and facilitate practical
        collaboration between Korean capabilities and global markets.
      </p>
      <p class="section-copy who__copy">
        With a strong focus on Saudi Arabia, Arab markets and the wider Middle
        East, KoreaIN facilitates market access, strategic partnerships and
        cross-border business development through a relationship-driven approach.
      </p>

      <a class="who__cta" href="/about">
        <span class="who__cta-label">More About Us</span>
        <span class="who__cta-arrow" aria-hidden="true">
          <svg viewBox="0 0 24 24">
            <path d="M5 12h14M13 6l6 6-6 6"></path>
          </svg>
        </span>
      </a>
    </div>
  </div>
</section>

<style>
  .who {
    padding-block: clamp(5rem, 7vw, 7.5rem);
    background: var(--color-bg);
  }

  .who__grid {
    display: grid;
    width: min(calc(100% - (2 * var(--gutter))), 100rem);
    grid-template-columns: repeat(12, minmax(0, 1fr));
    gap: clamp(4rem, 5vw, 5rem);
    align-items: center;
  }

  .who__visual {
    position: relative;
    grid-column: 1 / span 7;
    width: 100%;
    aspect-ratio: 16 / 10;
    margin: 0;
    overflow: hidden;
    border: 1px solid var(--color-border);
    border-radius: 1.5rem;
    background: var(--color-surface-soft);
  }

  .who__visual img {
    position: absolute;
    inset: 0;
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .who__content {
    grid-column: 8 / -1;
    min-width: 0;
    max-width: 46rem;
  }

  .who__content .section-title {
    max-width: 18ch;
    margin-bottom: 0;
    font-size: clamp(2.5rem, 3.4vw, 3.25rem);
    font-weight: 700;
    line-height: 1.08;
    letter-spacing: -0.035em;
    text-wrap: balance;
  }

  .who__copy {
    max-width: 32.5rem;
    margin-top: 1.75rem;
    font-size: 1.125rem;
    line-height: 1.65;
  }

  .who__copy + .who__copy {
    margin-top: 1.25rem;
  }

  :global([data-theme='dark']) .who__copy {
    color: var(--color-on-dark-muted);
  }

  .who__cta {
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: fit-content;
    max-width: 100%;
    height: 3.25rem;
    margin-top: 2rem;
    padding: 0 1.4rem;
    gap: 0.75rem;
    border: 1px solid rgba(255, 255, 255, 0.16);
    border-radius: 11px;
    background: linear-gradient(135deg, #004fb8 0%, #00269e 52%, #000c7a 100%);
    color: #fff;
    box-shadow:
      0 4px 14px rgba(0, 14, 138, 0.16),
      inset 0 1px 0 rgba(255, 255, 255, 0.2);
    font-size: 0.95rem;
    font-weight: 600;
    letter-spacing: 0.01em;
    line-height: 1;
    text-decoration: none;
    cursor: pointer;
    transform: scale(1);
    transform-origin: center;
    transition:
      transform 400ms cubic-bezier(0.4, 0, 0.2, 1),
      box-shadow 400ms cubic-bezier(0.4, 0, 0.2, 1);
    will-change: transform;
  }

  .who__cta-label {
    color: #fff;
    white-space: nowrap;
  }

  .who__cta-arrow {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    flex: 0 0 auto;
    color: #fff;
  }

  .who__cta:hover,
  .who__cta:focus-visible {
    transform: scale(1.08);
    box-shadow:
      0 10px 26px rgba(0, 14, 138, 0.26),
      inset 0 1px 0 rgba(255, 255, 255, 0.28);
  }

  .who__cta:focus-visible {
    outline: 3px solid var(--focus-ring);
    outline-offset: 3px;
  }

  .who__cta svg {
    width: 1.125rem;
    height: 1.125rem;
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.8;
  }

  @media (prefers-reduced-motion: reduce) {
    .who__cta {
      transition: none;
    }
  }

  @media (min-width: 80rem) {
    .who__visual {
      height: clamp(30rem, 33vw, 33.75rem);
      aspect-ratio: auto;
    }
  }

  @media (max-width: 63.99rem) {
    .who__grid {
      grid-template-columns: 1fr;
      gap: clamp(2rem, 4.5vw, 3rem);
    }

    .who__visual {
      grid-column: 1 / -1;
      aspect-ratio: 16 / 9;
    }

    .who__content {
      grid-column: 1 / -1;
      max-width: 46rem;
    }
  }

  @media (max-width: 39.99rem) {
    .who__visual {
      aspect-ratio: 16 / 10;
    }

    .who__content .section-title {
      font-size: clamp(2rem, 7vw, 2.375rem);
    }
  }
</style>
